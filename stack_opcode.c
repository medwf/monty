#include "monty.h"
/**
 * push - a function that pushes an element to the stack
 * @stack: a double pointer to linked list
 * @line_number: an integer represent line number
 */
void hundle_push(stack_t **stack, unsigned int line_number)
{
    (void)stack;
    (void)line_number;
    printf("-> %d", gs.int_to_push);
}